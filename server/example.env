# ============================================
# Configuração do Backend SGI
# ============================================
# Copia este ficheiro para .env e ajusta os valores

# ============================================
# Base de Dados
# ============================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sgi"

# ============================================
# Segurança (OBRIGATÓRIO em produção)
# ============================================
# Gerar secrets seguros:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="change-me-in-production-generate-strong-secret"
JWT_REFRESH_SECRET="change-me-refresh-in-production-generate-strong-secret"

# ============================================
# Servidor
# ============================================
PORT=5801
NODE_ENV=development

# URL do frontend (para CORS)
# Em desenvolvimento: http://localhost:8081
# Em produção: https://sgi.exemplo.pt
FRONTEND_URL=http://localhost:8081

# URL da API (para Swagger)
API_URL=http://localhost:5801/api

# ============================================
# Multi-tenant
# ============================================
# Tenant padrão usado quando não há header x-tenant-id
DEFAULT_TENANT_ID="tenant-default"

# ============================================
# Upload de Ficheiros
# ============================================
# Diretório base para uploads (relativo ao projeto)
# Os ficheiros são organizados por tenant automaticamente
UPLOAD_DIR="./uploads"

# Tamanho máximo de ficheiro (em bytes)
# Default: 10MB
MAX_FILE_SIZE=10485760

# ============================================
# Rate Limiting
# ============================================
# Configurado automaticamente:
# - Dev: 300 req/min
# - Prod: 100 req/min
# Ajustar em server/src/middleware/rateLimiter.ts se necessário

# ============================================
# Logging
# ============================================
# Níveis: error, warn, info, debug
# Em produção, apenas 'error' é recomendado
LOG_LEVEL=info
